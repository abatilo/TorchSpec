## Conversation
The user asked for the smallest real regression test for the DFlash2 offline artifact writer. The test had to call _save_without_vocab_pruning, load model.safetensors, and compare fc.weight with context_proj.weight without changing production code.

## Actions
Imported safetensors.torch.load_file and _save_without_vocab_pruning. Added a temporary-directory round-trip test that loads the written SafeTensors artifact and compares the exported fc.weight value with the model context projection. Ran the new test, the full 19-test DFlash2 file, diff checks, and a no-op writer mutation that failed as expected.

## Errors & Mistakes
The first combined git add and git commit command failed with: fatal: Unable to create /Users/abatilo/abatilo/TorchSpec/.git/worktrees/TorchSpec/index.lock: Operation not permitted. Staging was rerun with the required filesystem permission and succeeded. Ruff was not installed in .venv or on PATH, so lint and format checks could not run.

## Dead Ends
None.

## Hints for Future Agents
Keep the offline test at the file boundary. A helper-only assertion does not prove that model.safetensors is written or that the saved tensor values survive serialization.

## Codebase Discoveries
The offline no-vocabulary-pruning path writes model.safetensors through safetensors.torch.save_file after applying _prepare_export_tensors.

## Open Questions
None.
