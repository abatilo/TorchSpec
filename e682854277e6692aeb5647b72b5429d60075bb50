## Conversation
The user asked for PR #176 to be faithful to the public DFlash2 references and validated end to end. They requested implementation-only development without new tests or documentation.

## Actions
Changed DFlash2 serving state conversion to omit embed_tokens.weight, which is copied and frozen from the target model but absent from canonical DFlash2 checkpoints. Routed offline conversion through the model-specific serving-state transform so online model methods and tools/convert_to_hf.py emit the same key schema. Ran git diff --check, Ruff lint, and Ruff format checks.

## Errors & Mistakes
The original PR exported the frozen Qwen3-8B embedding, adding about 1.16 GiB and changing vLLM from target-embedding sharing to draft-owned embedding behavior. No error occurred while making this checkpoint.

## Dead Ends
Keeping the embedding because SGLang ignores unexpected weights was rejected. The public z-lab model, canonical safetensors, SGLang model contract, and vLLM sharing path all omit it.

## Hints for Future Agents
After training, convert the DCP checkpoint and verify embed_tokens.weight and lm_head.weight are absent while DFlash2 convolution and selector tensors are present. Load the actual artifact through merged SGLang PR #35371 at block size 8.

## Codebase Discoveries
DFlash2 owns embed_tokens only as a frozen training input. Its serving engines share the target embedding and LM head. The existing offline converter did not invoke model-specific serving-state transforms.

## Open Questions
The converted checkpoint still needs strict missing/unexpected-key validation and an actual serving load after the FULL_SHARD training run.
