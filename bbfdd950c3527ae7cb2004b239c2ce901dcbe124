## Conversation
The user asked to make PR #176 ready with the smallest final change and to use prior DFlash and DSpark work as the quality bar. They requested no new tests or documentation during development.

## Actions
Removed the PR's unrelated D-PACE valid-prefix behavior change. Restored the generic Trainer serving-save implementation to the upstream base because the requested non-decode DFlash2 recipe uses the established offline converter and did not justify a cross-trainer distributed error-policy rewrite. Ran git diff --check, Ruff lint, and Ruff format checks.

## Errors & Mistakes
The original PR accumulated a D-PACE behavior change and a broad generic serving-save rewrite even though neither was required for DFlash2 training. No error occurred while removing them.

## Dead Ends
Keeping the generic serving rewrite for hypothetical online decoding was rejected because configs/sglang_qwen3_8b_dflash2.yaml does not enable train_with_decode. The mature DFlash export contract uses tools/convert_to_hf.py.

## Hints for Future Agents
Keep the DFlashModel extension hooks used by DFlash2, but do not change D-PACE weighting. Validate DFlash2 through its distributed checkpoint and the offline converter.

## Codebase Discoveries
PR #134 and PR #140 established the serving key-remap and offline conversion path. DFlash2 only needs to extend that artifact contract.

## Open Questions
The offline DFlash2 artifact still needs strict conversion and an actual SGLang load after training.
