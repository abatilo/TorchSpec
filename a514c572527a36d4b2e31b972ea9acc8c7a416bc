## Conversation
The user asked to make TorchSpec PR #176 truly ready, keep development changes minimal, avoid tests and documentation until Qwen3-8B training is validated, and preserve both online and offline DFlash2 serving artifacts.

## Actions
Added a narrow serving-artifact writer that invokes model-specific config and state-dict transforms. Routed both successful full-state export and the existing fallback through it. Kept the existing rank and barrier behavior unchanged. Ran Ruff format, Ruff check, and git diff --check before the commit. Requested independent source and serving-contract reviews.

## Errors & Mistakes
The preceding scope-narrowing commit reverted a broad generic serving rewrite but also removed the only calls to DFlash2 config_for_serving and state_dict_for_serving from the online export path. Critics caught that the committed offline fix alone did not satisfy the PR's online-export promise.

## Dead Ends
The broad rank/error synchronization rewrite from the original PR was not restored because the requested training recipe does not need it and it changed generic Trainer behavior. Direct model.save_pretrained was rejected because it emits the internal DFlash2 schema.

## Hints for Future Agents
The exact training recipe has train_with_decode=false, so this code is not on the Qwen3-8B training path. Validate it later with a real DFlash2 serving artifact. A fallback write failure can still leave peers waiting at the inherited final barrier; this commit deliberately does not expand generic error policy.

## Codebase Discoveries
DFlash2 is the only current draft model with config_for_serving and state_dict_for_serving hooks. The public artifact schema needs model_type qwen3, nested DFlash2 configuration, serving key names, and no private target embedding.

## Open Questions
The optional vocabulary-pruning converter path has an inherited stale call signature and does not define a safe DFlash2 full-vocabulary selector remap. The exact Qwen3-8B configuration uses the full vocabulary, so resolve that after the requested training validation.
